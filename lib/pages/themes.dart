import 'package:flutter/material.dart';

// Define the colors based on the image
class AppColors {
  static const Color navbarBackground = Color(0xFF8B3A3A); 
  static const Color textHighlight = Color.fromARGB(255, 230, 13, 13); 
  static const Color iconUnselected = Colors.grey;
}
