import 'package:flutter/material.dart';

// Define the colors based on the image
class AppColors {
  static const Color navbarBackground = Color(0xFF8B3A3A); 
  static const Color textHighlight = Colors.purple; 
  static const Color iconUnselected = Color(0xFF007FFF);
}
